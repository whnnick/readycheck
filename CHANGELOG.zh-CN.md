# 更新日志

## 未发布

## 0.1.87 - 2026-08-31

- 官方 Codex app-server 发出 `account/rateLimits/updated` 时及时刷新额度，并保持 macOS 与 Windows 行为一致。
- 保留现有每分钟轮询，作为旧版本兼容和监听断线时的兜底。
- 对连续事件做防抖，并等待正在进行的刷新结束后再读取官方额度，全程不调用模型推理。
- Windows 便携包不再包含测试文件和仅用于构建的 Node 依赖。

## 0.1.86 - 2026-08-23

- macOS 与 Windows 同步保留官方多额度桶名称和服务端限额状态。
- 区分额度用尽、Credits 用尽、工作区或成员使用上限，不再继续显示通用“可用”。
- 明确官方本机 Codex 接口为首选数据源，只读 OAuth 用量端点为兼容回退。

## 0.1.85 - 2026-08-12

- macOS 提醒发送后回查通知中心，只有找到同一条通知才记录为成功。
- 在 macOS 偏好设置中增加通知状态、测试通知和打开系统通知设置入口。
- 将 0.1.85 以前仅由 API 接收确认的成功记录降级为“旧版状态不可确认”，不再冒充真实验证结果。
- Windows 预览版同步增加测试通知和打开通知设置入口。

## 0.1.84 - 2026-08-11

- 读取本机所有可用的 Codex app-server，仅从同一账号的其他候选源补齐缺失的主动重置详情。
- 当前官方来源暂时不返回详情时，跨重启复用此前已验证且尚未过期的重置时间；官方明确返回 0 时仍会立即清除。
- 主动重置详情暂不可用或被限流时，保持 macOS 与 Windows 行为一致。

## 0.1.83 - 2026-08-11

- macOS 与 Windows 同步新增本机通知记录，展示投递状态、尝试时间、重试次数和提醒详情。
- 同一提醒投递失败后重试只更新一条逻辑记录，最多保留最近 50 条，避免列表重复堆积。
- 旧版提醒节点迁移后明确标记为“状态不可确认”，不把缺少证据的旧记录误报为系统已接收。

## 0.1.82 - 2026-08-11

- 持久化已经验证的未来主动重置过期时间；官方详情源暂时缺少重置数据时，仍继续执行 72/48/24/12 小时提醒。
- 只有操作系统确认接收通知后才将提醒节点记为已发送；投递失败会保留并在后续刷新时重试。
- 仅在接口明确返回主动重置为 0 时清除已保存的过期时间，并保持 macOS 与 Windows 行为一致。

## 0.1.81 - 2026-08-09

- 对每个仍可使用的主动重置，在到期前 72、48、24、12 小时分别提醒。
- 每个提醒节点跨应用重启只触发一次；主动重置使用后停止后续提醒。
- 应用关闭期间跨过多个节点时，仅补发当前最紧急的一条，避免集中弹出多条通知。
- 兼容迁移原有三天提醒状态，升级后不会重复发送 72 小时提醒，并保持 macOS 与 Windows 行为一致。

## 0.1.80 - 2026-08-06

- 区分主动重置明确为 0 与重置详情暂时不可读取两种状态。
- 官方详情端点临时限流时，保留仍未过期的上一次成功读取结果。
- macOS 与 Windows 不再把未知重置数据误报为 0，并明确提示详情或过期时间暂不可用。
- 主额度继续每分钟刷新，重置与 Token 补充详情降为每 15 分钟读取一次；手动刷新仍会强制尝试读取详情。

## 0.1.79 - 2026-08-05

- 主动重置额度进入到期前 3 天时提醒一次。
- 经验证的 Codex 额度用尽且 Credits 余额开始下降时提醒；同一轮额度耗尽只提醒一次，额度恢复后自动允许下一轮提醒。
- 提醒状态跨启动持久化，macOS 与 Windows 行为保持一致，且不新增模型请求。
- macOS 启动时若用户尚未选择通知权限，会立即请求一次，不再等到首次提醒事件发生时才弹出。

## 0.1.78 - 2026-07-30

- macOS 每日 Token 柱状图支持鼠标悬停，显示完整日期和精确 Token 数。
- 将 Windows 对应的柱体详情提示纳入 UI 回归检查，保持双平台行为一致。

## 0.1.77 - 2026-07-30

- 将菜单栏弹层完整限制在 macOS 菜单栏下方，避免标题和顶部圆角被裁切。

## 0.1.76 - 2026-07-29

- 当本机 Codex 与 ReadyCheck 登录邮箱一致时，优先通过官方本地 app-server 读取额度窗口、Credits、重置过期时间和账户 Token 使用量。
- app-server 不可用、协议不兼容或账户不一致时，安全回退到现有 OAuth 只读用量端点。
- 根据 Codex 实际返回动态展示经过验证的额度窗口，不再硬编码某个 5 小时或 7 天窗口已停用。
- macOS 与 Windows 同步新增官方 7/30/90 天 Token 使用看板，同时保留本地额度下降看板作为降级方案。
- macOS 刘海动态选择当前可用额度窗口，并同步 Windows 行为。
- app-server 返回少量 stdio 数据时立即读取，不再等待长期运行的进程退出而触发超时回退。

## 0.1.75 - 2026-07-25

- 从主界面额度详情和详细 Widget 中移除重复的主动重置次数。
- 每一次主动重置的过期时间均单独展示，并同步 macOS 与 Windows 行为。

## 0.1.74 - 2026-07-25

- 从现有 OAuth 用量响应中读取并验证 Codex Credits 余额或无限额度状态，在主界面额度详情和详细 Widget 中显示。
- 极简 Widget、菜单栏和 macOS 刘海继续只突出当前额度百分比，并同步 Windows 客户端。

## 0.1.73 - 2026-07-25

- 修正 macOS 刘海可用状态圆点：仅在 Codex 当前额度数据有效且仍有剩余额度时显示绿色，额度用尽或数据不可用时显示灰色。
- 增加额度可用、用尽和未知状态的回归测试，并同步 Windows 包版本。

## 0.1.72 - 2026-07-22

- 将 macOS 刘海中 Codex 旁的圆点明确为二态数据状态：当前额度数据可用时显示绿色，否则显示灰色；额度紧张程度仅由百分比和进度条的颜色表达。
- Windows 包版本同步更新；刘海状态仍属于 macOS 专属硬件集成。

## 0.1.71 - 2026-07-22

- 点击桌面空白区域或其他应用时自动收起顶部菜单，避免菜单栏图标拥挤时无法关闭面板。
- 更新 GitHub README 头图，展示 macOS 刘海配额状态和当前 7 天额度模型，且不暴露账号信息。

- 修复刘海进度条在 7 天额度正常时错误显示为白色的问题，现与主界面额度进度条统一显示为绿色。

## 0.1.70 - 2026-07-22

- 修正当前 Codex 配额口径：macOS 与 Windows 统一展示仍在使用的 7 天额度，并隐藏已取消的 5 小时额度。
- 刘海状态条、Widget、菜单栏和本地使用看板统一读取经过验证的 7 天窗口，同时保留 5 小时解析与历史兼容能力。

## 0.1.69 - 2026-07-22

- 新增与系统检测到的硬件刘海对齐的 macOS 刘海额度状态条，展示当前 5 小时百分比、语义颜色进度条，支持点击打开主界面并自动识别刘海内置屏幕。
- macOS 和 Windows 界面不再展示已取消的 Codex 7 天额度，同时保留解析与历史兼容能力，便于未来恢复。
- 主窗口使用静态玻璃表面替代滚动时重复合成的材质模糊，并将无刷新情况下的全页时间更新从每秒一次降至每 30 秒一次，改善上下滚动流畅度。
- Windows 包版本同步更新；刘海状态条属于 macOS 专属硬件集成。

## 0.1.68 - 2026-07-18

- macOS 预览包在本机存在 ReadyCheck 稳定签名身份时会固定使用该身份，避免重新打包或升级后因 ad-hoc 应用身份变化而失去 Keychain 访问权限。
- 发布 App 会在本机临时目录完成签名，`dist` 只保留最终 DMG，避免同步目录附加的元数据破坏中间 App 副本的签名验证。
- DMG 会在同步目录之外生成并验证，完成后再统一替换 `dist` 中的旧文件，避免出现 `macos 2.dmg` 这类编号冲突副本。
- OAuth 已完成但凭据无法保存时，状态改为准确的“凭据保存失败”，不再误显示“授权失败”，并保留重新授权操作。
- Windows 安装包版本继续与 macOS 预览版保持一致。

## 0.1.67 - 2026-07-16

- 使用 HyperFrames 和真实产品截图重做 GitHub README 预览动图，让产品展示更清晰。
- macOS 与 Windows 的 Codex OAuth 请求同步当前 Codex CLI 流程：取消强制重新登录，并加入预期的来源参数。
- 在处理服务端错误前先校验 OAuth 回调 state，解析结构化 token 错误，并保持用户提示简洁，不记录凭据或回调数据。
- 区分网络、无效 token 响应和凭据保存失败；macOS 授权成功但无法保存凭据时，会明确引导用户解锁“登录”钥匙串。
- 明确区分本地 OAuth 回调监听器正在启动、已监听和不可用状态，不再提前假定监听已就绪。

## 0.1.66 - 2026-07-15

- macOS 与 Windows 的极简、详细 widget 现在会一致显示额度偏低、额度很低和额度用尽提醒。

## 0.1.65 - 2026-07-15

- macOS 与 Windows 在额度显示为 0% 时改为明确提示“额度用尽，等待重置”，不再误显示为额度偏低。
- 近期使用柱状图仅在首次出现时从 0 增长到目标值，自动刷新不再重复播放动画。
- 横轴明确保持“较早到最新”的时间顺序，并将额度窗口的蓝色选中框改为统一、克制的卡片底色。
- 统一 24 小时、7 天和 30 天范围的柱宽，历史数据较少时不再按类别空间拉伸柱形。
- 每个非零消耗柱都显示紧凑的百分比标签，不再只标注最高柱。
- Windows 近期使用图同步 macOS：按已观测时段布局、固定柱宽、仅首次播放入场动画、显示百分比标签，并统一卡片选中样式。
- 双端柱状图在最高消耗柱上方保留余量，避免最高柱贴顶后让用户误以为当前顶部刻度是使用上限。
- 双端刷新后仅对新增或增长的柱播放增量动画，未变化的柱保持静止；切换时间范围时只做短暂淡入。

## 0.1.64 - 2026-07-15

- 将容易产生歧义的“剩余额度折线”改为可切换 5 小时 / 7 天窗口的消耗柱状图；柱形显示检测到的额度下降百分点，当前剩余额度单独展示。
- 成功刷新默认按固定 1 分钟时间桶保存本地记录，最多保留 30 天；长时间刷新断档、额度重置和恢复不归入消耗。

## 0.1.63 - 2026-07-14

- 修复近期使用趋势采样：每分钟自动刷新时，跨过固定 5 分钟边界会新增历史点，不再无限覆盖同一条记录。

## 0.1.62 - 2026-07-14

- 新增按错误类型显示的额度恢复操作：临时刷新失败只需重试，只有授权明确失效或无法刷新时才提示重新登录；新授权成功前保留原凭据。
- 新增本地近期使用趋势看板，可查看 24 小时、7 天和 30 天的额度百分比变化，不将其误称为 Token 用量。
- 以 5 分钟为粒度在本机保存最多 30 天的最小化额度历史；历史不保存账号标识、凭据或原始接口响应。
- 保持 macOS 与 Windows 的恢复逻辑、看板范围和发布版本一致。
- 修正 Windows 打包脚本在 git worktree 中的输出位置，使其与 macOS 一样发布到仓库根 `dist`。

## 0.1.61 - 2026-07-03

- 在 `apps/windows` 启动 Windows 桌面客户端，包含 Electron 托盘、主窗口、悬浮 widget 壳层、本地偏好设置和 fail-closed 安全刷新状态。
- 为 Windows 预览版接入 Codex OAuth 回调流程和 Electron `safeStorage` 加密 token 存储；额度读取仍保持 fail-closed，等待解析器移植后再展示真实额度。
- 为 Windows 预览版接入只读 Codex usage 拉取和额度解析，支持 5 小时 / 7 天额度、套餐续期和主动重置信息；缺少关键数据时继续 fail-closed。
- 新增 Windows 黑盒测试手册和 `npm run smoke` 预检脚本，方便在 Windows 10/11 真机上验证预览版。
- 新增 Windows 便携版打包脚本，输出可复制到 Windows 运行的当前版本 `ReadyCheck-<version>-windows-x64-portable.zip`。
- 在公开文档中说明 Windows 便携预览包可作为 GitHub Release 资产下载，用于 Windows 10/11 测试。
- 修复 Windows 预览版 widget 置顶切换、极简/详细切换和详细模式内容裁切问题，并在 OAuth 授权中补充地区限制提示。
- 新增 Windows UI 行为 smoke test，打包前自动验证 widget 布局约束、样式切换和 OAuth 提示。
- 压缩 Windows 主窗口和 widget 的字体、间距与窗口尺寸，降低常驻 widget 对桌面的遮挡。
- 新增 `marketing/remotion` Remotion 产品介绍动效工程，提供 ReadyCheck 中文和英文 30 秒视频 composition。
- 为 Remotion 产品介绍动效新增程序生成的科技感电子背景音乐。
- 重做产品介绍动效的宣传片视觉：强化 GitHub 地址展示，加入与软件本体一致的绿/橙/红额度状态，并修复 OAuth 用量流程图线条覆盖文字的问题。
- 替换为更克制的高级科技感背景音乐。
- 新增从产品介绍视频生成的轻量 README 动态预览。
- 将 README 预览动图替换为真实 ReadyCheck 界面截图素材，并新增专用的高冲击力 GitHub 预览 composition。
- 优化 README 预览动图，移除 GitHub 地址叠层，并确保所有文案不再遮挡真实应用界面。
- 为 README 预览动图加入重新设计的菜单栏面板，并缩小主窗口展示比例，让产品形态更完整可读且不暴露用户账号信息。
- 将 macOS 额度紧急程度阈值与 Windows 对齐：低于 25% 为很低，25-49% 为偏低，31% 剩余不再显示为绿色。

## 0.1.60 - 2026-07-02

- 主窗口更新提示新增“稍后提醒”，可隐藏当前版本提示，但不影响手动检查更新。
- 将主窗口更新提示按钮改为“查看更新”，因为 ReadyCheck 当前打开 GitHub Releases，并不在应用内直接安装。
- 调整低额度阈值：剩余 10-24% 为偏低，低于 10% 为很低；widget 中低额度提醒聚焦 5 小时额度。

## 0.1.59 - 2026-07-02

- 详细 widget 中的主动重置过期时间支持换行展示，避免长日期在紧凑宽度下被遮挡。
- 为 Codex OAuth 回调失败和等待回调状态增加“取消授权 / 重新授权”操作，不再需要退出应用后重进。
- 确认软件更新检测仍连接到启动时静默检查和手动 GitHub Releases 检查。

## 0.1.58 - 2026-07-02

- 将 Windows 预览版应用版本号和便携版 zip 文件名与 macOS 发布版本统一。
- 从只读 `/wham/rate-limit-reset-credits` 详情端点读取 Codex 主动重置过期时间，避免 `/wham/usage` 只返回次数时过期时间显示为“未提供”。

## 0.1.57 - 2026-07-02

- macOS 与 Windows 解析器均识别 Codex `rate_limit_reset_credits.available_count`，用于显示当前可用的主动重置次数。

## 0.1.56 - 2026-06-30

- 让详细悬浮 widget 聚焦用户可读的额度和订阅信息，隐藏来源与验证元信息，同时主窗口继续保留这些详细信息。

## 0.1.55 - 2026-06-30

- 在菜单栏面板加入同样的“极简 / 详细”widget 样式切换，并与悬浮 widget 复用同一个切换组件。

## 0.1.54 - 2026-06-30

- 在悬浮 widget 的 header 区新增紧凑的“极简 / 详细”样式切换按钮。
- 固化标准发布流程，并让 DMG 打包前自动清理 `dist` 中的旧 ReadyCheck DMG。

## 0.1.53 - 2026-06-30

- 新增公开真实场景验收清单，覆盖首次安装、OAuth、安全刷新、更新检查、widget 位置和异常场景。
- 增加更新检查回归断言，确认 GitHub API 请求的 User-Agent 会带上 ReadyCheck 版本。

## 0.1.52 - 2026-06-30

- 隐藏顶部控制区里 widget 样式选择器的可见标题，避免中文界面被挤压后再次竖排显示。
- 应用启动恢复 widget 时使用与“显示 widget”和“重置位置”一致的右下角默认位置。
- 将“显示 widget”“置顶 widget”和“Widget 样式”三组控件拉开间距，并确保每组标题都保持单行。
- 统一详细 widget 中账号信息行的字号，不再只缩小套餐摘要行。

## 0.1.51 - 2026-06-29

- 将 widget 默认位置进一步下移，同时保留右侧边距。
- 在顶部 widget 控制区增加持久化的“极简 / 详细”样式选择，默认仍为极简。
- 详细 widget 模式会恢复套餐、续期、重置、来源和验证状态等信息。

## 0.1.50 - 2026-06-29

- “显示 widget”重新打开时使用和“重置位置”一致的右下角默认位置。
- 没有 widget 窗口时不再发送隐藏状态回调，避免显示开关被误同步。

## 0.1.49 - 2026-06-29

- 修复主窗口 Widget 偏好设置控件被挤压后中文标签竖排显示的问题。

## 0.1.48 - 2026-06-29

- 主窗口在发现 GitHub Release 新版本时显示非打扰式更新提示。
- 菜单栏面板新增“检查更新”和“下载更新”操作。
- 简化桌面 widget 的紧凑额度卡片，将套餐等详细信息保留在主窗口。
- 当某个额度窗口剩余低于 25% 时显示低额度提醒。
- 将偏好设置整理为语言、刷新、Widget 和软件更新分区。

## 0.1.47 - 2026-06-29

- 增加轻量 GitHub Release 更新检查；支持手动检查更新，并在发现新版本时提供下载入口。

## 0.1.46 - 2026-06-29

- 在紧凑额度卡片中隐藏来源和验证状态元信息，让桌面 widget 聚焦额度数值。

## 0.1.45 - 2026-06-29

- 主动重置次数始终按数字显示；未拿到次数数据时回退为 `0`，不再显示“未提供”。

## 0.1.44 - 2026-06-29

- 从额度卡片和桌面 widget 移除重复账号邮箱，让有限空间优先展示额度信息。
- 主动重置过期时间仅在主窗口显示，并改为普通账号详情行，不再使用额外黑色边框块。
- 当用量数据明确返回空的主动重置列表时，主动重置次数显示为 `0`。
- 在保留账号详情可读性的前提下，收窄上一版过宽的主窗口。
- 恢复点击桌面 widget 内容区域打开主界面的行为，同时避免刷新和关闭按钮误触发主窗口。

## 0.1.43 - 2026-06-29

- 将桌面 widget 恢复为原来的紧凑宽度，并在窄宽度下使用纵向布局展示 Codex 账号和套餐详情。
- 保留主窗口可滚动和加宽布局，保证新增账号信息可读。

## 0.1.42 - 2026-06-29

- 将 Codex 额度卡片重做为更接近截图的紧凑账号摘要布局：provider 胶囊、账号标题、套餐摘要行、主动重置块和额度进度行。
- 主窗口改为可滚动并加宽，避免邮箱和额度详情被截断。

## 0.1.41 - 2026-06-29

- 在额度卡片中增加 Codex 账号、套餐、续期时间和主动重置详情；字段可用时直接显示。
- 未验证或上游未提供的套餐/主动重置字段明确显示为“未提供”，不猜测数值。

## 0.1.40 - 2026-06-26

- 每次启动应用时都默认开启“显示 widget”，不再继承上一会话隐藏 widget 的状态。
- 移除桌面 widget 透明面板的原生阴影，修复外圈毛刺；保留 SwiftUI 的圆角柔和阴影。

## 0.1.39 - 2026-06-26

- 本版将“显示 widget”默认恢复为开启，即使升级前保留了旧版的关闭状态；在本版本中手动选择的开关状态仍会持续保存。
- 增加首个公开源码发布所需资料：可重复生成的 DMG 打包脚本、双语安装说明、安全策略和 Issue 表单。

## 0.1.38 - 2026-06-26

- 将“显示 widget”“置顶 widget”“重置位置”移回主窗口上方操作区域，避免下方设置说明被遮挡。
- 关闭“置顶 widget”时只降低 widget 层级，不再立刻把它重新置前。

## 0.1.37 - 2026-06-26

- 使用新的偏好键后，“显示 widget”默认开启。
- “置顶 widget”关闭时改用普通窗口层级，不再忽略鼠标事件，因此 widget 仍可拖动。
- 将“显示 widget”“置顶 widget”“重置位置”集中放在主窗口和菜单栏面板的同一组控制中。
- 通过“显示 widget”重新打开 widget 时，使用和“重置位置”一致的右下角默认位置。

## 0.1.36 - 2026-06-26

- 移除主窗口纵向滚动区域，让设置页更像固定的 macOS 工具窗口。
- 放大主窗口说明文字和额度元信息字号，提升阅读舒适度。
- 调整主窗口初始尺寸，使其和固定设置布局一致。
- 拖动 widget 不再打开主窗口；只有明确单击 widget 才打开主窗口。

## 0.1.35 - 2026-06-26

- “显示 widget”改为真正的持久化开关：关闭开关会关闭 widget，关闭 widget 也会同步关闭开关。
- “置顶 widget”关闭时，widget 保留在桌面层但不拦截桌面图标点击。
- 明确只有开启“置顶 widget”时才能直接拖动 widget。

## 0.1.34 - 2026-06-26

- 将 widget 操作文案从“固定 widget”改为“显示 widget”，让文案和实际行为一致。
- “显示 widget”改为幂等操作：重复点击只会置前并校正已有 widget，不再切换隐藏或重新创建。
- 增加“置顶 widget”设置：开启后 widget 浮在应用窗口前方，关闭后保留在桌面层。
- 保留“重置位置”作为明确的恢复操作：打开 widget 并放回屏幕可见区域右下侧。

## 0.1.33 - 2026-06-26

- Codex 登录后继续保留账号说明文字，并用已连接态文案说明 Keychain 存储和安全刷新行为。
- Codex 已连接后隐藏“连接”按钮，避免用户误以为需要反复授权；账号卡片只保留“断开”作为已连接态操作。

## 0.1.32 - 2026-06-26

- 拖动悬浮 widget 后会记住位置，重新打开时会恢复到目标屏幕内，并在主窗口和菜单栏弹出面板中增加“重置位置”操作。
- 额度数据过期时明确显示“已过期”，不再继续显示“可用”，避免用户误读旧数据。
- 菜单栏弹出面板顶部显示当前连接账号摘要。

## 0.1.31 - 2026-06-26

- 修复悬浮 widget 贴到右下角屏幕外的问题：使用固定 widget 尺寸，并在显示前后使用同一个目标屏幕可见区域进行约束。

## 0.1.30 - 2026-06-26

- 在主窗口恢复简洁的软件说明，同时保持额度状态为第一优先级。
- 移除账号区域重复显示的“已连接”状态。
- 从主窗口固定 widget 时重新创建悬浮窗口，避免隐藏或位置异常后需要点击两次。
- 在菜单栏弹出面板中增加“退出 ReadyCheck”操作。

## 0.1.29 - 2026-06-25

- 将主窗口改为额度状态优先的布局，账号和偏好设置下移，减少授权说明对已登录用户的干扰。
- 修复关闭悬浮 widget 后，从主窗口重新固定需要点击两次的问题。

## 0.1.28 - 2026-06-25

- 修复悬浮 widget 跑到屏幕外的问题：新建或重新打开时先选择当前活动屏幕，再将最终窗口 frame 限制在屏幕可见区域内。

## 0.1.27 - 2026-06-25

- 将 OAuth email 明确定义为 Codex 登录账号，主窗口不再把内部 account ID 作为登录账号兜底显示。

## 0.1.26 - 2026-06-25

- 登录 Codex 后，在主窗口 OAuth 卡片中显示当前账号标识；优先显示 OAuth token 中的邮箱，必要时才回退到账号 ID。

## 0.1.25 - 2026-06-25

- 启动或从主窗口重新打开悬浮 widget 时，将位置限制在屏幕可见区域内，避免跑到屏幕外。
- 增加标准“关闭窗口”命令，使 `Command-W` 可以关闭主窗口，不再触发系统错误音。

## 0.1.24 - 2026-06-25

- 将主窗口产品说明改为更清晰的用户视角文案。
- 菜单栏入口改为纯图标，并且点开菜单面板时不再激活主应用窗口。
- 主窗口和桌面 widget 使用同一套明确的绿色/橙色/红色额度进度条渲染。

## 0.1.23 - 2026-06-25

- 在主窗口加入简要产品说明，解释 ReadyCheck 的安全额度监控范围、凭据存储方式和可用展示入口。

## 0.1.22 - 2026-06-25

- 将额度刷新选项改为 1、3、5 分钟，并默认 1 分钟；刷新不会调用模型推理接口。
- 将 widget 和菜单中的“自动更新”改为“自动刷新”。
- 提升悬浮 widget 和额度卡片在浅色桌面背景下的文字对比度。

## 0.1.21 - 2026-06-25

- 增加“关于”窗口，显示版本、安全刷新行为、OAuth 用量数据来源和精度策略。
- 在 macOS 应用菜单和 ReadyCheck 主窗口中加入“关于”入口。
- 点击“固定 widget”时会将 widget 重新定位到主屏右下角并提升显示层级，避免拖到其他屏幕后找不到。

## 0.1.20 - 2026-06-25

- 悬浮 widget 改为无系统标题栏的桌面面板，并在 widget 内提供刷新和隐藏控制，不再只显示标题栏红叉。
- widget 默认放在屏幕可见区域右下侧并保留边距；主窗口改为不透明背景；长周期重置时间显示日期+时间；主窗口可见时也会自动刷新。

## 0.1.19 - 2026-06-25

- 主窗口从普通设置表单改为更接近已确认 Liquid Glass 方向的玻璃卡片布局。
- 菜单栏入口改为图标加 `ReadyCheck` 文本，并在启动后显示桌面悬浮 widget。

## 0.1.18 - 2026-06-25

- 连接 Codex 账号后，ReadyCheck 主窗口直接显示额度卡片。
- 已连接账号打开主窗口时自动刷新额度，并在额度区提供手动刷新按钮。

## 0.1.17 - 2026-06-25

- 增加 `localhost:1455` 本地 OAuth 回调监听，浏览器完成 Codex 授权后可自动回到应用完成授权。
- 本地回调端口不可用时，仍保留手动粘贴回调 URL 的兜底流程。

## 0.1.16 - 2026-06-24

- 恢复菜单栏 ReadyCheck 入口，同时保留前台主窗口用于设置和授权。
- 菜单面板中的“设置”和“连接”改为打开显式主窗口，不再依赖 SwiftUI settings scene。

## 0.1.15 - 2026-06-24

- 在 `static main()` 中完成启动后立即创建主窗口，不再依赖 delegate 回调。

## 0.1.14 - 2026-06-24

- 暂时移除启动阶段菜单栏 status item 初始化，优先保证双击后主窗口可见。

## 0.1.13 - 2026-06-24

- 将主窗口创建延迟到主 runloop，并强制前置显示。

## 0.1.12 - 2026-06-24

- 自定义 AppKit main 中显式调用 `finishLaunching()`，确保启动 delegate 创建主窗口。

## 0.1.11 - 2026-06-24

- 将启动层切换为传统 AppKit 生命周期，显式创建主窗口和菜单栏 status item。

## 0.1.10 - 2026-06-24

- 使用 AppKit 显式创建 ReadyCheck 主窗口，修复 SwiftUI `WindowGroup` 未可靠显示的问题。

## 0.1.9 - 2026-06-24

- 启动时显式切换为普通前台 app 并激活窗口，修复运行后没有可见反馈的问题。

## 0.1.8 - 2026-06-24

- 启动时打开 ReadyCheck 主窗口，避免纯菜单栏 app 看起来“没反应”。
- 本地打包不再设置后台-only 模式，Dock 和应用切换器中会显示 ReadyCheck。
- 在 worktree 中打包时默认输出到可见的仓库 `dist/ReadyCheck.app`。

## 0.1.7 - 2026-06-24

- 将本地验收包输出到 `dist/ReadyCheck.app`，避免用户从隐藏 `.build` 目录启动。
- 为本地 app bundle 生成并声明 ReadyCheck 图标。

## 0.1.6 - 2026-06-24

- 优化 Codex v1 菜单空状态：未连接时优先展示连接引导，而不是自动生成未授权错误卡。
- 优化 Codex OAuth 设置页：增加三步授权说明，明确浏览器授权、粘贴回调 URL 和刷新行为。
- 优化额度卡片：将百分比明确标记为“剩余”，并在有 reset 时间时显示重置时间。

## 0.1.5 - 2026-06-24

- 聚焦 Codex-only MVP，默认只启用 Codex OAuth provider。
- 增加本地 `.app` 打包脚本和 Codex v1 黑盒验收文档。
